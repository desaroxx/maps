var planner;
(function (planner) {
    var PlannerMapController = (function () {
        function PlannerMapController() {
        }
        return PlannerMapController;
    })();
    angular.module("planner").controller("plannerMapCtrl", PlannerMapController);
})(planner || (planner = {}));
